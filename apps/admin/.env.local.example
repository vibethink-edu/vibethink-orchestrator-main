# ------------------------------------------------------------------
# VIBETHINK ADMIN CONSOLE - LOCAL ENVIRONMENT CONFIG
# ------------------------------------------------------------------

# 1. SUPABASE CONNECTION (Required)
# Get these from your Supabase Dashboard > Settings > API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJh... (Your Public Anon Key)

# 🚨 CRITICAL SECURITY 🚨
# This key bypasses RLS. Never commit this file.
SUPABASE_SERVICE_ROLE_KEY=eyJh... (Your Secret Service Role Key)

# 2. DEVELOPMENT OVERRIDES (Optional)
# If set, this bypasses DB Role lookup. Use only for quick local testing.
# clear this value or comment it out to test REAL DB AUTH (Recommended).
# ADMIN_DEV_ROLE=SUPER
