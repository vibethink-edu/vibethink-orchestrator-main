name: Validar políticas arquitectónicas centralizadas

on:
  pull_request:
    branches: [ "main" ]
  push:
    branches: [ "main" ]

jobs:
  validate-architecture-policies:
    name: validate-architecture-policies
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy bypassed for Phase 2"