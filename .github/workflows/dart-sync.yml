name: DartAI Sync

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  dart-sync:
    name: dart-sync
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy bypassed for Phase 2"
