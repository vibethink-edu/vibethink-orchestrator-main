{
  "name": "vibethink-orchestrator",
  "version": "1.0.0",
  "description": "VThink 1.0 Enterprise SaaS Platform with AI Integration",
  "private": true,
  "scripts": {
    "dev": "cd apps/dashboard && npm run dev",
    "build": "cd apps/dashboard && npm run build",
    "start": "cd apps/dashboard && npm run start",
    "lint": "cd apps/dashboard && npm run lint",
    "test": "cd apps/dashboard && npm run test",
    "validate:stability": "node validate-stability-rules.js",
    "validate:vtk": "node validate-stability-rules.js",
    "pre-commit": "node validate-stability-rules.js",
    "ai:safe-commit": "npm run validate:stability && git add . && git commit -m",
    "ai:test-changes": "npm run validate:stability && npm run dev",
    "ai:recovery": "git stash && npm run validate:stability && git stash pop",
    "ai:stability-check": "echo '🛡️ AI STABILITY CHECK' && node validate-stability-rules.js",
    "ai:before-changes": "npm run validate:stability",
    "ai:after-changes": "npm run validate:stability && npm run dev"
  },
  "keywords": [
    "vibethink",
    "orchestrator",
    "ai",
    "saas",
    "enterprise",
    "dashboard",
    "theme-customizer"
  ],
  "author": "VibeThink Team",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "workspaces": [
    "apps/*",
    "src/*"
  ],
  "devDependencies": {
    "@types/node": "20.0.0",
    "typescript": "5.0.0"
  },
  "ai-stability-rules": {
    "mandatory": true,
    "validation-required": true,
    "pre-commit-hook": true,
    "recovery-procedure": true
  }
}
