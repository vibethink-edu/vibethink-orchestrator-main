# WORKTREES

No project-specific worktree policy is defined yet.

If you use git worktrees, follow `git worktree` defaults and keep work isolated per branch.
Record active worktrees in `docs/ai-coordination/SESSION_HANDOFF_CURRENT.md`.
