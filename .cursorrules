# ============================================================================
# VIBETHINK ORCHESTRATOR 1.0 - AI AGENT CONFIGURATION
# ============================================================================
# Inherits from: _vibethink-dev-kit
# Last updated: 2025-12-16
# ============================================================================

<identity>
You are working on **VibeThink Orchestrator 1.0**, an Enterprise SaaS Platform.
All critical rules are in `AGENTS.md` (single source of truth).
Universal rules are in `_vibethink-dev-kit/knowledge/ai-agents/AGENTS_UNIVERSAL.md`.
</identity>

<mandatory_first_actions>
# ============================================================================
# 🔥 MANDATORY FIRST ACTIONS - EXECUTE BEFORE ANY TASK
# ============================================================================

## When Starting ANY Session:

### Step 1: Check Project Scripts
```bash
list_dir project_root/scripts/   # Find operational scripts
read_file scripts/start-dashboard.ps1  # Start script
read_file scripts/stop-dashboard.ps1   # Stop script
```

### Step 2: Read Configuration
```bash
read_file AGENTS.md              # Project rules (this project)
read_file package.json           # Available npm scripts
```

### Step 3: Report Available Commands
**Tell user what operations are available before proceeding.**

## ⚡ QUICK OPERATIONS (USE THESE)

| Action | Command | Port |
|--------|---------|------|
| **Start Dashboard** | `.\scripts\start-dashboard.ps1` | 3005 |
| **Stop Dashboard** | `.\scripts\stop-dashboard.ps1` | - |
| **Build** | `npm run build:dashboard` | - |
| **Dev (npm)** | `npm run dev:dashboard` | 3000 |

**CRITICAL:** Use project scripts, NEVER guess ports or commands.
</mandatory_first_actions>

<user_information>
The USER's OS version is Windows.
The user has active workspaces defined in AGENTS.md.
You are not allowed to access files not in active workspaces.
</user_information>

<tool_calling>
Call tools as you normally would. The following list provides additional guidance:
- **Absolute paths only**. When using tools that accept file path arguments, ALWAYS use the absolute file path.
- Check AGENTS.md for project-specific tool usage patterns.
</tool_calling>

<communication_style>
- **Formatting**: Use GitHub-style markdown for better readability.
- **Proactiveness**: Be proactive in completing tasks, but avoid surprising the user.
- **Helpfulness**: Explain your work clearly and acknowledge mistakes.
- **Ask for clarification**: If unsure about the USER's intent, always ask.
- **Report discoveries**: When starting, tell user what scripts/commands you found.

See AGENTS.md for project-specific communication guidelines.
</communication_style>

<project_rules>
## VibeThink Orchestrator Specific Rules

### Tech Stack (DO NOT CHANGE)
- **Core**: React 19.0.0, TypeScript 5.9.2
- **Build**: Next.js 15.3.4 (App Router)
- **Styling**: Tailwind CSS 4.1.10
- **UI**: Shadcn UI via `@vibethink/ui`
- **Backend**: Express 4.21.2 (when needed)

### ❌ NEVER Do (Project-Specific)
- Install Express 5 (use 4.21.2)
- Install `vite` (this is Next.js)
- Use `@vibethink/bundui-ui` in production
- Run npm commands inside `apps/` directories
- Guess port numbers without checking scripts

### ✅ ALWAYS Do (Project-Specific)
- Use `@vibethink/ui` for UI components
- Run commands from monorepo root
- Use `scripts/*.ps1` for operations
- Check AGENTS.md for workflows

### Monorepo Structure
```
apps/dashboard/      # Main dashboard (Next.js)
packages/ui/         # @vibethink/ui (Shadcn components)
packages/utils/      # Shared utilities
scripts/             # Operational PowerShell scripts
```
</project_rules>

<user_rules>
The following are user-defined rules that you MUST ALWAYS FOLLOW WITHOUT ANY EXCEPTION.

## ⚠️ CRITICAL: Read AGENTS.md First

**BEFORE doing ANYTHING in this project:**
1. Read `AGENTS.md` in the project root
2. Follow ALL rules defined there
3. Check `scripts/` directory for operational commands

`AGENTS.md` contains:
- Quick Operations table (START/STOP/BUILD)
- Tech stack details
- Workflow guidelines
- Project-specific rules

**AGENTS.md is the SINGLE SOURCE OF TRUTH for this project.**

## Universal Rules (Inherited from Dev-Kit)

See `_vibethink-dev-kit/knowledge/ai-agents/AGENTS_UNIVERSAL.md` for:
- Mandatory First Actions
- Universal Prohibitions/Requirements
- Session Continuity Protocol
- Stability Rules

### Code Quality
- ✅ No hardcoded paths (use environment variables)
- ✅ No secrets in code (use .env files)
- ✅ All dependencies must be justified
- ✅ No duplicate code between projects

### Git Workflow
- ✅ Verify no extra worktrees before major operations
- ✅ Use conventional commits
- ✅ Never commit to main directly (use branches)

### Documentation
- ✅ Update CHANGELOG.md for significant changes
- ✅ Document breaking changes
- ✅ Keep AGENTS.md updated

</user_rules>

<workflows>
## Development Workflow

1. **Start:** `.\scripts\start-dashboard.ps1` or `npm run dev:dashboard`
2. **Code:** Follow stack guidelines
3. **Verify:** Check browser for errors
4. **Build:** `npm run build:dashboard`
5. **Commit:** Follow pre-commit checklist in AGENTS.md

## Pre-Commit
Ask user: "¿Actualizamos la versión antes de hacer commit?"

For complete workflows, see AGENTS.md.
</workflows>

<ephemeral_message>
There will be an <EPHEMERAL_MESSAGE> appearing in the conversation at times. This is not coming from the user, but instead injected by the system as important information to pay attention to.

Do not respond to nor acknowledge those messages, but do follow them strictly.
</ephemeral_message>

# ============================================================================
# INHERITANCE CHAIN
# ============================================================================
# 
# This project (.cursorrules) inherits from:
# 1. _vibethink-dev-kit/.cursorrules.template (base template)
# 2. _vibethink-dev-kit/knowledge/ai-agents/AGENTS_UNIVERSAL.md (universal rules)
# 
# Project-specific rules: AGENTS.md (in this project root)
# 
# ============================================================================
