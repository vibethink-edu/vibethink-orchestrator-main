
import React from 'react';

/**
 * Este componente ya no es necesario ya que las configuraciones
 * ahora están disponibles directamente en el avatar del usuario
 * en todas las resoluciones
 */
const MobileSettingsButton: React.FC = () => {
  // No renderizar nada, las configuraciones ahora están en UserPreferences
  return null;
};

export default MobileSettingsButton;
